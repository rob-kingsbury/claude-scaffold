{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Example global MCP configuration for ~/.claude/settings.json",

  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-filesystem", "~/projects"],
      "description": "File system access for projects directory"
    },

    "github": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-github"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}"
      },
      "description": "GitHub repository operations"
    },

    "brave-search": {
      "command": "npx",
      "args": ["-y", "@anthropic/mcp-server-brave-search"],
      "env": {
        "BRAVE_API_KEY": "${BRAVE_API_KEY}"
      },
      "description": "Web search via Brave Search API"
    },

    "context7": {
      "command": "npx",
      "args": ["-y", "@upstash/context7-mcp"],
      "description": "Up-to-date documentation for libraries and frameworks"
    }
  },

  "notes": {
    "env_vars": "Use ${VAR_NAME} syntax - Claude Code will read from environment",
    "paths": "Use absolute paths or ~ for home directory",
    "security": "Keep API keys in environment variables, not in this file"
  }
}
